import React, { FC } from "react";
import ConnectedLayout from "UI/connectedLayout";

const HomePage: FC = () => (
    <ConnectedLayout>
        <h1>Accueil</h1>
    </ConnectedLayout>
);

export default HomePage;
